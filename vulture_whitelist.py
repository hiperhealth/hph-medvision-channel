# Vulture whitelist — suppress false positives for unused code detection.
# Add entries here when vulture incorrectly flags code that IS used
# (e.g., hook methods called by the framework, not by us directly).
#
# Format: one Python expression per line that vulture should ignore.
# See: https://github.com/jendrikseipp/vulture#whitelisting
